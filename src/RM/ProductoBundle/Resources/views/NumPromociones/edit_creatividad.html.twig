<div class="animated fadeInDown" data-instancia="{{ instancia.idInstancia }}" data-grupo="{{ grupo.idGrupo }}">
    {{ form_start(form) }}

    <div class="content">
        {% include '@RMProducto/NumPromociones/form_creatividad_edit.html.twig'  with {'form': form} %}
    </div>

    <table cellpadding="0" cellspacing="0" border="0">
        <tbody>
        <tr>
            <td width="100"><button type="submit" class="btn btn-primary">{{ "boton.guardar" |trans|title}}</button></td>
        </tr>
        </tbody>
    </table>

    {{ form_rest(form) }}
    {{ form_end(form) }}
</div>